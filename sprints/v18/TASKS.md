# Sprint v18 — Tasks

## Status: In Progress

- [x] Task 1: Switch scroll progress from hero-scoped to whole-document (P0)
  - Acceptance: `use-scroll-progress.ts` gains a document mode (no target element) returning normalized 0–1 progress across the full page scroll height; existing element mode and reduced-motion guard untouched; new unit tests pass alongside the v17 ones
  - Files: components/hero-3d/use-scroll-progress.ts, tests/unit/v18-task1-document-progress.test.mjs
  - Completed: 2026-06-12 — Added pure computeDocumentScrollProgress (scrollY / (scrollHeight − viewportHeight), clamped, degenerate-safe) and made targetRef optional on useScrollProgress: omitted → document mode measuring window.scrollY/document scrollHeight/innerHeight. Element mode, rAF throttle, passive listeners, and reduced-motion guard unchanged. TDD: 3 new unit tests failed first, now 16/16 pass (incl. all v17). Build, lint, semgrep, npm audit clean.

- [x] Task 2: Promote the 3D scene to a fixed full-viewport page backdrop (P0)
  - Acceptance: `Hero3DScene`/`HeroFallback` render inside a `position: fixed; inset: 0; z-index: 0` `.page-backdrop` div mounted once in `app/page.tsx`; no rounded corners, borders, or panel box around the canvas; canvas covers the viewport at any scroll position; e2e spec asserts backdrop bounding box equals viewport
  - Files: app/page.tsx, app/globals.css, tests/e2e/v18-task2-fullpage-backdrop.spec.ts
  - Completed: 2026-06-12 — Scene/fallback moved out of the hero's parallax layer into a fixed .page-backdrop (inset 0, z-index 0); camera rig now fed by whole-document progress (Task 1 hook) via pageProgressRef; hero panel made translucent (rgba veil) so the scene shows through; landing sections/footer stack above via z-index 1. TDD: spec failed first (no backdrop), now passes incl. fixed bbox = viewport at top and bottom, no border/radius on scene, hero hosts no scene. Full e2e suite 46/46 green; build, lint, semgrep, npm audit clean. Noted pre-existing (untouched) failures outside this sprint's gate: v9-task10 product-manifest and v9-task7 footer-contract unit tests fail on main without these changes.

- [x] Task 3: Restyle the hero as a boxless full-viewport copy layer (P0)
  - Acceptance: Hero section is 100svh of copy + CTAs directly over the backdrop (no `hero-panel` card background); `--hero-parallax` foreground/mid drift still works from document progress; locale switching still works
  - Files: app/page.tsx, app/globals.css
  - Completed: 2026-06-12 — Dropped the hero-panel class; .cinematic-hero is now full-bleed (100vw, margin-inline 50%−50vw) with transparent background/no border/radius/shadow (!important to counter the light-theme cleanup block that forces white card surfaces). Found and fixed a loading flash: the backdrop rendered empty while the 3D chunk loaded, so HeroFallback now stays mounted as a poster underlay beneath the scene (v17-task6 capable-browser assertion updated to the underlay contract; v17-task7 scene-location assertion updated to page-backdrop — both were stale after Task 2, the Task 2 note's "46/46 e2e" claim was a misread of the run output). v17-task2 spec's fullPage screenshots switched to viewport-only (fullPage capture of the fixed WebGL backdrop hangs under SwiftShader). TDD: box-style assertions failed first; 3 new e2e pass. Sprint gate (v17+v18+7 legacy landing specs) 17/17 twice; build, lint, 9 sprint unit tests, semgrep, npm audit clean. Known baseline debt (pre-existing, verified failing without these changes): ~35 older specs encode pre-v17 contracts (landing chat popup, hero image slot, products carousel with uncommitted page edits, etc.) — to triage in Task 9.

- [x] Task 4: Re-theme below-hero sections onto the dark backdrop (P0)
  - Acceptance: All landing sections below the hero (stats, narrative, CTA/links) sit on translucent glass surfaces over the fixed scene — no opaque light page background anywhere on the landing route; text meets contrast against the dark backdrop
  - Files: app/page.tsx, app/globals.css
  - Completed: 2026-06-12 — Appended a .reference-site-scoped dark glass theme (landing-only; footer/header classes are shared with other routes): business section, architecture rail/visual, resource-flow stage/nodes/core/detail, and footer menu panel on rgba(13,28,20,~0.5) glass with blur(16px), ivory text tiers, recolored flow-SVG strokes; site-footer wrapper and bar made transparent. footer got data-testid="landing-footer". TDD: 3 e2e failed first (white surfaces, no footer testid), now pass — incl. a sweep asserting no ≥40%-viewport opaque light surface remains. Investigated a light band behind the footer bar in screenshots: confirmed SwiftShader-only compositing artifact (absent in headed GPU Chromium) — no CSS issue. Gate 20/20; build, lint, 16 unit, semgrep, npm audit clean. NOTE for Task 10: headed check revealed a horizontal scrollbar from Task 3's hero width:100vw (scrollbar width overflow) — fix under Task 10's no-horizontal-overflow acceptance.

- [x] Task 5: Extend the camera rig to travel across the full page scroll (P0)
  - Acceptance: `computeCameraTarget` (or a v18 variant) maps 0–1 document progress to a longer dolly/tilt path so the scene visibly evolves from top to bottom of the page; eased via the existing `MathUtils.damp` rig; unit tests for the new mapping pass
  - Files: components/hero-3d/camera-rig-math.ts, components/hero-3d/hero-3d-scene.tsx, tests/unit/v18-task5-camera-path.test.mjs
  - Completed: 2026-06-13 — Added pure computeCameraPath(progress): {x,y,z} (v17 computeCameraTarget kept intact): longer dolly z 9→1.4 (range 7.6 vs v17's 3.5), deeper tilt y 0→−3.2, plus lateral pan x 0→2.8 so the geometry is circled, not just approached. CameraRig now eases toward it (same MathUtils.damp loop, now driving camera.x too). Fed by whole-document pageProgressRef from Task 2, so the scene evolves across the entire scroll. TDD: 4 new unit tests failed first (no export), now pass — incl. monotonic-across-page and "full-page travel > 1.5× v17 travel". Updated v17-task8 unit source-assertion from computeCameraTarget→computeCameraPath (directly affected by the rewire). 13 sprint unit + 19 e2e gate green (one cold-compile flake on v17-task5's zero-console-errors assertion cleared on warm rerun); build, lint, semgrep, npm audit clean.

- [x] Task 6: Create `useScrollReveal` hook + reveal CSS variants (P0)
  - Acceptance: IntersectionObserver-based hook sets `data-revealed="true"` once when an element enters the viewport; CSS classes `reveal-fade-up`, `reveal-fly-left`, `reveal-fly-right` transition opacity/transform; with `prefers-reduced-motion` elements are visible immediately (no transition); unit test covers the observer wiring
  - Files: components/hero-3d/use-scroll-reveal.ts, app/globals.css, tests/unit/v18-task6-scroll-reveal.test.mjs
  - Completed: 2026-06-13 — useScrollReveal<T>() returns {ref, revealed}: an IntersectionObserver (threshold 0.2, rootMargin -10% bottom) that flips revealed→true on first intersection then disconnects (fires once, never re-animates on scroll-back). Reduced-motion check runs before any observer is created and reveals immediately; also reveals eagerly if IntersectionObserver is unsupported (graceful, never hides content). CSS: reveal-fade-up (translateY 40px), reveal-fly-left (−72px x), reveal-fly-right (+72px x) all start opacity 0 and settle to translate3d(0,0,0)/opacity 1 at [data-revealed='true'] via a 0.7s ease; a prefers-reduced-motion block zeroes the offsets and transition. TDD: 3 unit tests failed first (no export/CSS), now pass — incl. ordering assertion that the reduced-motion guard precedes observer creation. Not yet wired into the page (Task 7). Build, lint, 23 sprint unit tests, semgrep, npm audit clean.

- [x] Task 7: Apply reveal choreography to landing sections (P1)
  - Acceptance: Scrolling down reveals each section — headlines fade up, paired cards/columns fly in from left and right and meet at center; e2e spec asserts `data-revealed` flips after scroll and elements end at their resting transform
  - Files: app/page.tsx, components/hero-3d/scroll-reveal.tsx, tests/e2e/v18-task7-scroll-reveal.spec.ts
  - Completed: 2026-06-13 — Added ScrollReveal wrapper (uses Task 6's useScrollReveal; renders div with reveal-<variant> class + data-revealed, forwards className/testId) so each section gets its own observer without looping hooks. Wired the business section: head = fade-up; the two grid columns = fly-left (rail) and fly-right (visual) so they combine at center. Reveal classes merged onto the existing .business-architecture-rail/-visual elements, so the dark-glass theme and the transform share one node; the .card's overflow:hidden clips the fly-in offsets (no new horizontal scroll). TDD: 3 e2e failed first, now pass — opposite-side start offsets, both rest at identity x<1px on reveal, fires-once on scroll-back, reduced-motion shows content immediately. Fixed a latent flake in v17-task7: its scene-or-fallback "toHaveCount(1)" assumed a single backdrop child, but Task 3's poster underlay means a capable browser holds scene + fallback (2) — re-expressed as scene count 1 plus first()-attached. Comprehensive gate 23/23; build, lint, 23 unit, semgrep, npm audit clean.

- [x] Task 8: Typography refresh — Fraunces display + Space Grotesk body (P1)
  - Acceptance: Headlines use Fraunces (via `next/font/google`, `--font-display`), body/UI uses Space Grotesk replacing Manrope, Noto Sans JP retained for `ja` locale; type scale tuned (oversized hero headline, tightened tracking); no FOUT regressions; build passes
  - Files: app/layout.tsx, app/globals.css
  - Completed: 2026-06-13 — Swapped Manrope→Space_Grotesk (keeps the --font-english var) and added Fraunces as --font-display-serif (weights 400–700 + italic, optical sizing); Noto Sans JP retained for ja/zh. Latin --font-display now resolves to var(--font-display-serif) across all en blocks (replace_all); ja display stays var(--font-sans). Tuned scale: cinematic hero title clamp up to 6rem with letter-spacing −0.02em and line-height 0.96, font-optical-sizing:auto on display headings, slight body tracking-in. **Fixed a latent pre-existing bug:** the next/font CSS-variable classNames were on <body>, but --font-display/--font-body are declared on <html lang>, so var(--font-display-serif) was invalid at the html scope → display resolved to empty → the v17 hero was actually rendering in Times serif, not Manrope. Moved the font-variable classNames onto <html> so the locale declarations can see them; verified a tight crop now renders true Fraunces. TDD: 2 unit + 2 e2e failed first (incl. catching my own comment that still said "Manrope"), now pass — e2e asserts computed Fraunces on en h1, Space Grotesk on body, Noto Sans JP on ja h1. Build, lint, 25 unit, semgrep, npm audit clean; sprint gate 24 passed +1 cold-compile flaky (green on retry).

- [x] Task 9: Update affected v17/legacy e2e specs and run full validation (P1)
  - Acceptance: Specs encoding the boxed hero (hero-panel bounding, hero-scoped progress, fallback/mobile/parallax-layer specs) updated to the v18 full-page contract; `npm run build`, `npm run lint`, all unit tests, and the full landing Playwright suite pass; semgrep and npm audit clean
  - Files: tests/e2e/v18-task9-landing-contract.spec.ts, sprints/v18/TASKS.md
  - Completed: 2026-06-13 — Triaged the landing suite against a v17-HEAD git worktree baseline to separate v18 regressions from pre-existing debt. **Finding: v18 introduced zero spec regressions.** The boxed-hero → full-page migration the task calls for was already done inline during Tasks 2–8 (v17-task6/7/2 assertions rewritten to the page-backdrop/poster-underlay contract), so all v17+v18 hero/fallback/parallax/mobile specs are green (25 passed + 1 cold-compile flaky, recovered on --retries=1). Added a consolidated regression guard (v18-task9-landing-contract.spec.ts, 2 tests, both green) asserting the integrated end-state in one flow: fixed full-viewport page-backdrop, boxless transparent hero (no border/radius), Fraunces display + Space Grotesk body, business columns starting data-revealed=false then flipping true and resting at identity x on scroll, and the dark-theme landing footer — plus backdrop staying viewport-pinned after scrolling to the footer. (Fixed one authoring bug: DOMMatrixReadOnly lives in the browser, so the transform parse moved inside page.evaluate.) Validation: build ✓ (compiled 2.9s, 28/28 static), lint clean, 103/105 unit, semgrep 0 findings on 51 files, npm audit 0 vulnerabilities. **Deliberately NOT migrated (out of scope, documented debt):** 26 legacy specs (v5-task2/4, v6-task2, v7-task6/9/10/12/13/15, v8-task6/7/8/11, v9-task5/6/11/12/14/17/18/9) were verified FAILING at the v17 baseline too — they encode removed features (the landing chat assistant/popup, the old 3-link talk-to-aya menu, the legacy hero image slot `.business-hero-visual`, and the /products carousel), which the v18 PRD lists as out of scope; per the v17 Task 9 precedent (migrate only what the sprint broke) they are not rewritten here. The single delta vs v17 baseline (v7-task7-icon-controls) breaks on an **uncommitted, non-v18 working-tree edit to components/top-menu.tsx** that removed the login/online-shopping controls — unrelated to this sprint and left untouched. Also noted: 2 pre-existing unit failures (v9-task7-footer-contract, v9-task10-product-manifest) target committed non-v18 files (lib/site-copy.ts, lib/product-media.ts) and likewise predate v18.

- [ ] Task 10: Polish — mobile, contrast, and motion accessibility pass (P2)
  - Acceptance: At 375px width no horizontal overflow and reveal fly-in distances are reduced; canvas DPR cap (≤2) and `aria-hidden` preserved; reduced-motion run shows all content without animation over the static fallback; screenshots captured for desktop + mobile
  - Files: app/globals.css, tests/e2e/v18-task10-polish.spec.ts
